// Layout for /register/success.
// Intentionally omits the TopNav so this page renders as a standalone screen.
export default function RegisterSuccessLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
